torch>=2.0.0
torchvision
torchaudio
datasets>=2.14.0
tokenizers>=0.15.0
tqdm>=4.65.0
PyYAML>=6.0
tensorboard>=2.16.0
# TensorBoard <2.16 breaks with setuptools>=81 (pkg_resources.iter_entry_points)
setuptools>=65.0,<81
numpy>=1.24.0
# Cloud monitoring (RunPod): pip install wandb
wandb>=0.16.0
# Optional: BLEU metrics during validation
# sacrebleu>=2.3.0
